# Use custom image with requiments and dags.
images:
  airflow:
    repository: thautech/airflow-ssh-airbyte
    tag: 0.0.2

# Enable HTTP basic authorization.
extraEnv: |
  - name: AIRFLOW__API__AUTH_BACKENDS
    value: 'airflow.api.auth.backend.basic_auth'