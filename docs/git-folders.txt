flight
    airflow
    airbyte
    dbt
    superset

    nuxt
    fastapi

    sklearn
    tensorflow

    postgres (schema)
    minio (datasets)

    *kafka, redis, mongodb, ...

    docker
        build.sh
        Dockerfile.airflow
        Dockerfile.dbt-postgres (redshift)
        Dockerfile.nuxt-client
        Dockerfile.nuxt-server
        Dockerfile.fastapi

    kubernetes
        README.md (production considerations)
        helm (development)
            workflow -> airflow(dbt) + airbyte
            storage -> minio + pgadmin + postgres
            analytics -> superset
            compute -> spark
            webapp -> nginx + gunicorn|nodejs
            stream -> kafka (kraft)
            monitor -> prometheus


helm
    charts
        [DONE] nfs (bookworm?)
        [DONE] postgres (uuid + postgis)
        [DONE] minio
        [DONE] pgadmin (:latest)
        [DONE] jupiter (jupyter/pyspark-notebook:latest)
        pyspark (apache/spark-py)
        *redis, mongodb, ...


site (nuxt)

infra (terraform)