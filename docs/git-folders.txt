flight
    airflow
    airbyte
    dbt
    superset

    nuxt
    fastapi

    sklearn
    tensorflow

    postgres (schema)
    minio (datasets)

    *kafka, redis, mongodb, ...

    docker
        build.sh
        Dockerfile.airflow
        Dockerfile.dbt-postgres (redshift)
        Dockerfile.nuxt-client
        Dockerfile.nuxt-server
        Dockerfile.fastapi

    kubernetes
        README.md (production considerations)
        helm (development)
            workflow -> airflow(dbt) + airbyte
            storage -> minio + postgres(pgadmin)
            analytics -> superset
            compute -> spark
            webapp -> nginx + gunicorn|nodejs
            stream -> kafka (kraft)
            monitor -> prometheus


helm
    charts
        [DONE] nfs (bookworm?)
        [DONE] minio
        [DONE] postgres (uuid + postgis) / pgadmin (:latest)
        [DONE] jupiter (jupyter/pyspark-notebook:latest)
        *redis, mongodb, ...